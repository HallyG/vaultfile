name: PR

on:
  pull_request:
    branches: [master]
    types:
      - opened
      - synchronize

jobs:
  ci:
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    uses: ./.github/workflows/ci.yaml